{
  "name": "final",
  "version": "1.0.0",
  "description": "**node_modules folder was too large to import to GitHub: Please run npm install from this directory to reinstall all needed modules before running!**",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mjmccrac",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0"
  },
  "dependencies": {
    "ipfs-http-client": "^28.1.2",
    "truffle-hdwallet-provider": "^1.0.2",
    "web3": "^1.0.0-beta.37"
  }
}
